# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Tool caches
.pytest_cache/
.ruff_cache/

# hootie runtime artifacts
.hootie-cache/
# legacy paths, from before the rename to hootie
.pyqa-cache/
out/

# Local documents used for manual testing; never commit user documents
/scanned_doc.pdf

# Secrets. Never commit these.
.env
.env.*
!.env.example

# Your working config. Ship hootie.example.toml and examples/*.toml instead —
# this is the file a real API key eventually gets pasted into.
/hootie.toml
# legacy path, from before the rename to hootie
/pyqa.toml
